name: docker-compose-actions-workflow
on: push
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Build the stack
        run: docker-compose up -d
      - name: postgresdbrun
        run: docker run --name admin -e POSTGRES_PASSWORD=wannasucceed -p 5432:5432 -d postgres:latest
      - name: pgadminrun
        run: docker run -p 8080:80  -e "PGADMIN_DEFAULT_EMAIL=interviewee@test.com" -e "PGADMIN_DEFAULT_PASSWORD=wannasucceed"  -d dpage/pgadmin4
      - name: process
        run: docker-compose ps
      
        
      
